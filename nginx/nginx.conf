
server {

    listen 80;

    location /static {    
        autoindex on;    
        alias /srv/covidoff/staticfiles;    
    }
    
    location / {
        proxy_pass http://localhost:8000;
        proxy_redirect off;
    }
}
